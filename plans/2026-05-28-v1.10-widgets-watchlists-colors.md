# Build Plan: StartDashboard v1.10 — Widget reorder, multiple watchlists, per-card colors

**Date:** 2026-05-28
**Status:** Approved
**Plan author:** Design Agent (via Orchestrator)
**Approved by:** Tom (verbal approval of all three features; UI sub-decisions made by Orchestrator and surfaced to Tom)

## The Idea, In Plain English

Three enhancements to the live StartDashboard, all per-persona and synced to the gist:
1. Drag widget cards to reorder them (like categories and items already reorder).
2. Have more than one watchlist, each with its own custom title.
3. Give any category card or widget card a custom border/glow color, with a one-click reset to the persona's theme accent.

## What It Will Do (Behavior)

### Feature 1 — Widget card reorder
- Each widget card header gets a ⠿ grip (existing `.grip` pattern). Drag to reorder; saved to `persona.widgetOrder`.
- `renderWidgets` iterates `widgetOrder` (filtered by `hiddenWidgets`) instead of a fixed order.

### Feature 2 — Multiple, renamable watchlists
- `persona.watchlists` = array of `{ id, title, symbols[] }`.
- Migration: legacy singleton `widgets.watchlist` → `[{ id, title: 'Watchlist', symbols }]`. New personas default to one empty watchlist titled "Watchlist".
- Each watchlist renders as its own card (key `watchlist:<id>` in widgetOrder/hiddenWidgets/widgetColors), reusing the Finnhub `renderStockWidget`.
- Each watchlist's ⚙ config modal includes a **title rename field**, symbol management (existing), and a **delete this watchlist** button (confirm).
- **Markets stays a singleton** (SPY/QQQ/DIA index proxies) — not folded into watchlists.
- "+ Add watchlist" button lives in **Settings**, near the widget-visibility toggles. New watchlists default-titled "Watchlist 2" etc., empty, shown by default, appended to widgetOrder.
- Widget-visibility toggles list each watchlist by its title.

### Feature 3 — Per-card custom border/glow color
- `category.color` (optional hex; null = inherit accent). `persona.widgetColors = { <widgetKey>: hex }` (absent = inherit accent).
- Colored cards get a colored border + soft outer glow; uncolored cards keep the current accent-derived default.
- **Categories:** "Card color…" in the existing `...` context menu → popover with preset swatches + native color picker + **"Default (theme)"** reset.
- **Widgets:** every widget gets a ⚙ gear. Widgets with existing config modals (clock, weather, crypto, markets, watchlists) get a "Card color" control added. To-do and Notes get a new minimal config modal containing just the Card color control. Same swatches + custom + "Default (theme)" everywhere.

## What It Will NOT Do
- Markets does not become renamable or multiple (it's the index-proxy singleton).
- No per-card color animation/pulsing — a static subtle glow only.
- No new external dependencies.

## Files to Be Changed
| File | What |
|------|------|
| `projects/personal-dashboard/index.html` | All code: schema migration, widgetOrder iteration + widget grips, watchlists data model + rendering + config rename/delete + add-watchlist, per-card color data + CSS + pickers |

## Dependencies
None new. Finnhub (stocks), CoinGecko (crypto), Open-Meteo (weather), GitHub gist (sync) all unchanged.

## Open Questions for Tom
None — three UI sub-decisions resolved: (1) migrate legacy → one "Watchlist", new personas get one empty; (2) "+ Add watchlist" in Settings; (3) every widget gets a gear, To-do/Notes gear opens a color-only config.

## Unknowns (for Research Agent)
None.

## Definition of Done
1. Widget cards show ⠿ grips; dragging reorders them; order persists per-persona across reload.
2. Can add multiple watchlists, each with a custom title; each has its own symbols and its own ⚙ config (rename + symbols + delete).
3. Legacy single watchlist migrates cleanly to the new array shape with symbols preserved; an old-shape persona never crashes.
4. Markets unchanged and still works.
5. Any category or widget card can be given a custom border/glow color, and reset to "Default (theme)".
6. All three persist to the gist and sync to other browsers.
7. Documentation Agent has run and PROJECT-HUB.html, HANDOFF.md, MASTER-HUB.html, TECHNICAL.html, USER-EXPERIENCE.html all reflect v1.10.
